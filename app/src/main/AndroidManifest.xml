<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.np.mad.evap2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".forumchat"
            android:parentActivityName=".forum" ></activity>
        <activity
            android:name=".addforum"
            android:parentActivityName=".forum" />
        <activity
            android:name=".forum"
            android:parentActivityName=".discussionboard" />
        <activity
            android:name=".peoplelist"
            android:parentActivityName=".groupinfo" />
        <activity android:name=".groupinfo" />
        <activity android:name=".CreateTask" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".discussionboard" />
        <activity android:name=".account" />
        <activity android:name=".creategrp" />
        <activity android:name=".grpchat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>